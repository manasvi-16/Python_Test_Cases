#This pgm takes two strings concatenates them and returns to the user

def city_function(city,country):
    """Concatenates values inserted by user."""
    conc = f"{city}, {country}"
    return conc.title()
